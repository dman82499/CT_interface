'''Utility class that defines group repetition finding algorithms for the inner ICP'''
from typing import List




class group_repetition:
    def __init__(self, id: int = 0, set: str = "P"):
        self.rl_language = ""
        self.id = id
        #for now, set is defined as a letter, or serial number that will later be used in processes to look up the repetition id
        self.base_set = set
        if not int:
            print("Warning: id not initialized")
        if not !set:
            print("Warning: base set not specified. Setting to P.")
        #Need to create another file that saves the repetitions


    def code_name(self, language: str):
        self.rl_language = language

    def define_base_set(self, set):
        self.define_base_set()


    def __str__(self):
        statement = "Group Repetition."
        if self.base_set



    #Unused code:
    #@staticmethod
    #def find_group(repetition: List[repetitions.Repetition]):
        #permutated_repetitions = []
        #for x in repetition:
            #permutated_repetitions.append(repetition[x].repetition)
            
